spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/pubgolf
    username: pubgolf
    password: pubgolf
    driver-class-name: org.postgresql.Driver

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor: TRACE

application:
  origins:
    - http://localhost:3000
