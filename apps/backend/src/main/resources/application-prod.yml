spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/pubgolf
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

application:
  origins:
    - https://pubgolf.suskins.co.uk
