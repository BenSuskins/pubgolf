spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/pubgolf
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

application:
  origins:
    - https://pubgolf.suskins.co.uk
